DROP TABLE IF EXISTS words;

CREATE TABLE IF NOT EXISTS words (
    word_id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL unique,
    word VARCHAR(100) NOT NULL,
    description VARCHAR(5000) NOT NULL,
    parent_id INTEGER
);